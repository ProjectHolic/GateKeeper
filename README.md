# Work Distribution 

---

- UI and Integration 
- model
- policy
- services

# Details 

---
## 1 . UI and Integration 

Building UI and Integrate different state with the actual backend 
example if UI add some client then at backend `List<Client> clints` should have the same client added 
```text
    - See ObservableList and how to use them
    - uses of Screen Build
    - CSS
    - JavaFX Basic 
```

## 2 . Model
Implement all module inside the Module package such as `Client`, `Request`, `Abuse Report` and so on.

## 3. Policy
Implement RateLimit Policies such as `FixedWindow` & `SlidingWindow`.
## 4.  Services 

Implement services like `BurstGeanarate` or `Single Request`.

---

Additional Features will be decided accordingly # GateKeeper
# GateKeeper
